<Window x:Class="班级点名器.TopModeWindow_StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:班级点名器"
        mc:Ignorable="d"
        Title="TopModeWindow_StartWindow" Height="500" Width="800" WindowStartupLocation="CenterScreen"
        Background="Transparent" AllowsTransparency="True" WindowStyle="None" Loaded="Window_Loaded" Name="ThisWindow">
    <Grid>
        <Viewbox>
            <Grid>
                <Border Background="White" Opacity="0.4" Height="400" Width="1000">
                    <Border.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="75"/>
                        </Style>
                    </Border.Resources>

                </Border>
                <Label Name="Name" Height="300" Width="1000" Content=""  
                       FontSize="250" FontFamily="微软雅黑" FontWeight="Heavy" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
            </Grid>
        </Viewbox>
    </Grid>
</Window>
